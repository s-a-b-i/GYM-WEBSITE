<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Member</title>
  <style>
    body {
      background-color: #080710;
      color: #fff;
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      color: #3498db;
      font-size: canopee;
    }

    form {
      max-width: 400px;
      width: 100%;
      margin: 20px 0;
      padding: 20px;
      border: 1px solid #3498db;
      border-radius: 5px;
      background-color: #2c3e50;
    }

    label {
      display: block;
      margin: 10px 0 5px;
      font-weight: bold;
    }

    input {
      width: 100%;
      padding: 8px;
      margin-bottom: 10px;
      box-sizing: border-box;
      background-color: #34495e;
      border: 1px solid #3498db;
      color: #fff;
    }

    button {
      background-color: #4caf50;
      color: white;
      padding: 10px 15px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #45a049;
    }

    a {
      display: block;
      margin-top: 10px;
      color: #3498db;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    button.go-back {
      display: block;
      margin-top: 10px;
      background-color: #3498db;
      color: #fff;
      border: none;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
    }

    button.go-back:hover {
      background-color: #2980b9;
    }
  </style>
</head>
<body>
  <h1>Update Member Information</h1>

  <form action="/update" method="post">
    <input type="hidden" name="id" value="<%= member.id %>">

    <label for="name">Name:</label>
    <input type="text" name="name" value="<%= member.name %>" required>

    <label for="email">Email:</label>
    <input type="email" name="email" value="<%= member.email %>" required>

    <label for="address">Address:</label>
    <input type="text" name="address" value="<%= member.address %>" required>

    <label for="contactno">Contact Number:</label>
    <input type="text" name="contactno" value="<%= member.contactno %>" required>

    <button type="submit">Update</button>
  </form>

  <button class="go-back" onclick="window.location.href='/read'">Go back to read page</button>
</body>
</html>
